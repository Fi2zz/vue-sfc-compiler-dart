# style_keyframes

```
@keyframes spin-./style_keyframes.vue {
from { transform: rotate(0);
}
to { transform: rotate(360deg);
}
}
.a[data-v-./style_keyframes.vue] { animation: spin-./style_keyframes.vue 1s; animation-name: spin-./style_keyframes.vue;
}
```
