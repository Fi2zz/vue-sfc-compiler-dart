# style_keyframes_dup_names

```
@keyframes a-./style_keyframes_dup_names.vue {
from { opacity: 0;
}
}
@keyframes b-./style_keyframes_dup_names.vue {
from { opacity: 1;
}
}
.x[data-v-./style_keyframes_dup_names.vue] { animation: a-./style_keyframes_dup_names.vue 1s steps(2),b-./style_keyframes_dup_names.vue 2s; animation-name: a-./style_keyframes_dup_names.vue,b-./style_keyframes_dup_names.vue;
}
```
